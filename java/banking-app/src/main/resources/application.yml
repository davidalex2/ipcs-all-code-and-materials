spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bank_db
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: bank-account-service

server:
  port: 8082